@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Giriş Yap - Jointly</title>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>

<div class="login-page">
    <div class="row g-0 min-vh-100">
        <!-- Left Side - Image -->
        <div class="col-lg-6 d-none d-lg-block">
            <div class="login-image-side">
                <div class="login-overlay">
                    <div class="overlay-content">
                        <img src="~/images/logo.png" alt="Jointly" height="50" class="mb-4">
                        <h1 class="display-4 fw-bold text-white mb-3">Anılarınızı<br>Birlikte Yaşayın</h1>
                        <p class="lead text-white opacity-90">Her özel anınız, sevdiklerinizle paylaşıldığında daha değerli.</p>
                    </div>
                </div>
                <img src="~/images/login_hero.png" alt="Jointly" class="img-fluid w-100 h-100 object-fit-cover">
            </div>
        </div>

        <!-- Right Side - Login Form -->
        <div class="col-lg-6">
            <div class="login-form-side d-flex align-items-center justify-content-center p-4 p-lg-5">
                <div class="login-form-container">
                    <!-- Mobile Logo -->
                    <div class="d-lg-none text-center mb-4">
                        <img src="~/images/logo.png" alt="Jointly" height="50">
                    </div>

                    <div class="mb-5">
                        <h2 class="fw-bold mb-2">Hoş Geldiniz! 👋</h2>
                        <p class="text-muted">Jointly hesabınıza giriş yapın</p>
                    </div>

                    <!-- Login Form -->
                    <form>
                        <div class="mb-3">
                            <label for="email" class="form-label fw-semibold">E-posta</label>
                            <input type="email" class="form-control form-control-lg" id="email" placeholder="ornek@email.com" required>
                        </div>
                        <div class="mb-3">
                            <label for="password" class="form-label fw-semibold">Şifre</label>
                            <input type="password" class="form-control form-control-lg" id="password" placeholder="••••••••" required>
                        </div>
                        
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" id="rememberMe">
                                <label class="form-check-label small" for="rememberMe">
                                    Beni Hatırla
                                </label>
                            </div>
                            <a href="#" class="small text-decoration-none text-gradient-link">Şifremi Unuttum?</a>
                        </div>

                        <button type="submit" class="btn btn-primary w-100 btn-lg mb-3">Giriş Yap</button>
                    </form>

                    <!-- Divider -->
                    <div class="position-relative my-4">
                        <hr>
                        <span class="position-absolute top-50 start-50 translate-middle bg-white px-3 text-muted small">veya</span>
                    </div>

                    <!-- Social Login -->
                    <div class="d-grid gap-2 mb-4">
                        <button class="btn btn-outline-secondary btn-lg">
                            <i class="bi bi-google me-2"></i>Google ile Giriş Yap
                        </button>
                    </div>

                    <!-- Register Link -->
                    <p class="text-center mb-0 text-muted">
                        Hesabınız yok mu? 
                        <a href="@Url.Action("Register", "Account")" class="text-decoration-none fw-bold text-gradient-link">Kayıt Olun</a>
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .login-page {
        min-height: 100vh;
    }

    .login-image-side {
        position: relative;
        height: 100%;
        overflow: hidden;
    }

    .login-image-side img {
        object-fit: cover;
        height: 100%;
    }

    .login-overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(135deg, rgba(183, 110, 121, 0.85) 0%, rgba(224, 129, 117, 0.75) 100%);
        z-index: 1;
        display: flex;
        align-items: center;
        padding: 4rem;
    }

    .overlay-content {
        position: relative;
        z-index: 2;
    }

    .login-form-side {
        background: linear-gradient(135deg, var(--bg-light) 0%, var(--white) 100%);
        min-height: 100vh;
    }

    .login-form-container {
        width: 100%;
        max-width: 380px;
    }

    .form-label {
        color: var(--text-dark);
        margin-bottom: 0.5rem;
    }

    .form-control {
        border: 2px solid rgba(183, 110, 121, 0.15);
        border-radius: 12px;
        padding: 0.75rem 1rem;
        transition: all 0.3s;
    }

    .form-control:focus {
        border-color: var(--primary-color);
        box-shadow: 0 0 0 0.2rem rgba(183, 110, 121, 0.15);
    }

    .form-check-input:checked {
        background-color: var(--primary-color);
        border-color: var(--primary-color);
    }

    .text-gradient-link {
        background: var(--gradient-main);
        -webkit-background-clip: text;
        background-clip: text;
        -webkit-text-fill-color: transparent;
        font-weight: 600;
        transition: opacity 0.3s;
    }

    .text-gradient-link:hover {
        opacity: 0.8;
    }

    .btn-outline-secondary {
        border: 2px solid rgba(183, 110, 121, 0.2);
        color: var(--text-dark);
        font-weight: 500;
        transition: all 0.3s;
    }

    .btn-outline-secondary:hover {
        background: var(--gradient-soft);
        border-color: var(--primary-color);
        color: var(--text-dark);
    }

    @@media (max-width: 991.98px) {
        .login-form-side {
            min-height: auto;
            padding: 3rem 1.5rem;
        }
    }
</style>

<script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
